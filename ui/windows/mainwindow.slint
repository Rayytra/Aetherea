import { Button, HorizontalBox } from "std-widgets.slint";
import { WhatsOnTheMenu } from "../docks/menubar.slint";
export component MainWindow inherits Window {
    background: darkslategray;
    title: project_title;

    in property <string> project_title: "Aetherea - No Project";

    WhatsOnTheMenu {}
}
